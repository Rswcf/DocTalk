NEXT_PUBLIC_API_BASE=http://localhost:8000
# BACKEND_INTERNAL_URL=http://backend:8000
# ^ Production-only: Railway internal hostname. Server-side proxy + auth adapter
#   prefer this over NEXT_PUBLIC_API_BASE for server-only fetches. Leave unset locally.
AUTH_SECRET=your-32-byte-secret-here
ADAPTER_SECRET=your-adapter-secret-here
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

