This folder previously held static demo PDFs. Demo documents are now served from
the backend (seeded at startup from `backend/seed_data/`) and accessed via MinIO
presigned URLs. The old sample files (`10k-sample.pdf`, `paper-sample.pdf`,
`contract-sample.pdf`) have been removed.
