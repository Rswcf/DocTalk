SHIP

No blocking findings under the requested severity bar. Reviewed `9625e4d..b9c4bfd` against the backlog decision §9.22 and §9.24. One nonblocking structured-data coverage regression is recorded below.

**NOTE — the five alternatives detail pages lose their existing ItemList.**

Locations: `frontend/src/app/alternatives/chatpdf/page.tsx:34`, `frontend/src/app/alternatives/askyourpdf/page.tsx:34`, and the corresponding replacements on `/alternatives/humata`, `/alternatives/notebooklm`, and `/alternatives/pdf-ai`; the replacement resolver is exemplified by `frontend/src/app/alternatives/chatpdf/ChatpdfAltsJsonLd.tsx:13`.

Concrete sequence: at `9625e4d`, load `/alternatives/chatpdf` and extract its JSON-LD: it includes Article, FAQPage, ItemList, and BreadcrumbList. At `b9c4bfd`, the same page emits Article, FAQPage, and BreadcrumbList; ItemList is gone, although the visible alternatives remain. The same deletion occurs on all five alternatives detail pages. The old `alternatives` arrays remain unused in their page wrappers. This removes existing schema coverage but creates no false claim, build failure, or visible change, so it is a note, not a blocker.

Suggested fix: restore ItemList in the per-page resolvers using the rendered alternatives' names, order, and destinations, localizing DocTalk URLs as appropriate. If omission is intentional, document that decision and remove the unused arrays. Neither action is required for this SHIP verdict.

**Acceptance checks**

- **Rendered content, every locale:** rebuilt and checked all **242 URLs**: 22 paths × en/zh/ja/ko/es/de/fr/pt/it/ar/hi. All **770 FAQ question/answer pairs** match the rendered accordion text, count, and order. All **33 HowTo steps** match the rendered step titles and descriptions. Script, style, and template content is excluded from the visible-text comparison. No missing content or English FAQ/step fallback was found.
- **Headlines and descriptions:** I would include these in the content-consistency requirement. `Article.headline` should identify the rendered article; an application description may summarize visible content, but must not invent capabilities or use the wrong language. This follows [Google's content and relevance guidance](https://developers.google.com/search/docs/appearance/structured-data/sd-policies) and [headline definition](https://developers.google.com/search/docs/appearance/structured-data/article). This implementation passes the stronger exact-match check: all **242 headlines** equal the rendered H1, all Article descriptions equal the hero lede, and all **66 SoftwareApplication descriptions** equal that locale's rendered lede. Non-English descriptions differ from the English versions; the resolver-key test also confirms direct presence in each locale JSON, without fallback.
- **Citations HowTo:** `frontend/src/app/features/citations/CitationsJsonLd.tsx:13` resolves `howStep<n>Title` / `howStep<n>Desc`, the same existing keys used by `CitationsContent.tsx:32` and rendered at `CitationsContent.tsx:143`. All 11 routes pass, including Japanese. The section name and description also match. The obsolete FAQ wording is absent; all five actually rendered FAQs survive, including the English “Does it work with DOCX and PPTX?” question. This is a wording correction, not a reason to delete that rendered question.
- **Application offers and pricing deletion:** `frontend/src/app/pricing/PricingJsonLd.tsx:23` matches the actual pricing cards in all 11 languages: Free **$0**, Plus **$9.99**, Pro **$19.99**, USD. AggregateOffer has matching lowPrice/highPrice and offerCount 3; offer names and summaries match the rendered cards. Each of the four feature schemas with three offers matches those prices; free-demo correctly has only the zero-price offer. All 11 pricing routes emit exactly **Article + BreadcrumbList + SoftwareApplication**, with **no FAQPage**. Its content component, billing links, tracking, and metadata are unchanged.
- **Tools CollectionPage:** accepted. `frontend/src/app/tools/ToolsHubJsonLd.tsx:24` describes the collection of word-counter and reading-time tools rendered by `ToolsHubContent.tsx:28`. Its name/description match the hero in every locale, its URL matches that route's canonical, and `isPartOf` points to the corresponding DocTalk locale homepage. [Schema.org defines CollectionPage as a collection webpage](https://schema.org/CollectionPage); an ItemList is not mandatory. Also, the baseline English `tools/page.tsx` already emitted CollectionPage. Phase 2 preserves and localizes that type; it did not introduce it to English.
- **Duplication and scope:** every reviewed URL emits exactly one Article and one instance of each intended additional block. Every Article page URL matches the rendered canonical. Breadcrumb labels/destinations match the visible trail; application destinations and asset URLs are correct. `frontend/src/lib/marketingLocalePage.tsx:60` remains unchanged and selects the override instead of also emitting its generic Article. All phase 1 route/resolver files, `/use-cases/lawyers`, and `/features/layout-translation` are untouched. The shared component's existing Article/FAQ/Breadcrumb branches are unchanged. AST comparison confirms all 44 route-wrapper changes are confined to schema rendering/wiring, all 22 English metadata objects are unchanged, and no content, CSS, translation, or backend files changed.

**Verification performed**

- `npm run build`: passed, including compile, lint/type checking, and generation of 425 static pages.
- `npm run test:unit`: **68 passed, 0 failed**, including existing phase 1 coverage and the unchanged factory fallback route.
- `python3 scripts/verify-marketing-jsonld-phase2.py`: passed for all 242 URLs, 770 FAQ pairs, and 33 steps; inspected its DOM parsing to exclude script-content self-matches.
- Independent HTML checks additionally verified all canonicals, non-English text against English, application descriptions, actual displayed pricing-card amounts against every offer, aggregate values, and tools `isPartOf` destinations.
- Reconstructed the exact commit comparison by reading and hash-verifying repository objects with Python; **no git command was run**. All **636 tracked frontend files** match `b9c4bfd`, so the rebuilt output corresponds to the requested frontend revision. No source fixes or deployment were performed.
