BLOCK

1. **[BLOCK] Retry/Continue single-flight still races Send, so one rendered UI can launch and bill two chat requests.**
   - **Location:** `frontend/src/lib/useChatStream.ts:345` (with the competing Retry mutation at `frontend/src/lib/useChatStream.ts:433`; Continue has the same shape at `:471`).
   - **Failing sequence:** Render a failed-answer Retry button while `isStreaming === false`, with text already present in the composer. Invoke Retry and then submit the composer from that same render before React commits another render (the same programmatic/double-activation window as r1 F3). Retry acquires `regenerateOrContinueLatchRef`, mutates the transcript/accounting, and synchronously writes the Zustand streaming flag at line 433. The already-rendered `sendMessage` callback nevertheless tests its captured `isStreaming === false` at line 345; it neither reads the live store nor shares the latch, so it appends another turn and opens a second stream. Both authenticated requests can debit credits, while both streams target the global last assistant message. The reverse order is safe only because Retry reads the live store; Retry-first is not.
   - **Suggested fix:** Make `sendMessage` participate in the same synchronous admission gate, or at minimum replace its captured guard with `useDocTalkStore.getState().isStreaming` before any message/accounting mutation. Add a regression that invokes the same rendered Retry callback followed immediately by the same rendered Send callback and asserts one request and one accounting mutation.

2. **[BLOCK] Reparse's new User-before-Document lock order can deadlock against document deletion's Document-before-User settlement path.**
   - **Location:** `backend/app/api/documents.py:892` (User lock before the Document update at `:931`), versus `backend/app/services/doc_service.py:162`, `backend/app/services/predebited_job_service.py:126`, and `backend/app/services/extraction_service.py:625`.
   - **Failing sequence:** Let a ready document have a queued/running, unreconciled extraction-style `DocumentJob`. Start `DELETE` and let it lock the parent `Document` at `doc_service.py:162`. Concurrently start reparse: it locks `User` at `documents.py:892`, then reaches the conditional Document update at line 931 and waits for DELETE's parent lock. DELETE acquires advisory namespace 948 and awaits the fresh-session resolver launched at `predebited_job_service.py:126`; that resolver wins the conditional ledger delete and then waits to update the same `User` at `extraction_service.py:625`. The cycle is reparse `User -> Document`, delete `Document -> awaited resolver`, resolver `-> User`. Because one edge is the Python `await asyncio.to_thread(...)`, PostgreSQL cannot detect the whole cycle as a database deadlock; both user operations can hang instead of completing as they did before the reparse User lock was added.
   - **Suggested fix:** Establish one compatible lock order without making the parent-delete transaction await a second transaction that needs a lock it holds. A targeted option is to lock the reparsed Document first with `FOR NO KEY UPDATE` (so child FK inserts remain compatible), validate owner/status from that locked row, then acquire the User lock and perform the capacity count/conditional claim. This keeps different-document reparses serialized by User, preserves same-document 409 behavior, and makes reparse wait for parent deletion before it can hold User. Add a real-Postgres regression covering delete + reparse with an active unreconciled document job.

3. **[NOTE] A transport-level `fetch()` rejection still bypasses both the Retry discriminator and streaming cleanup.**
   - **Location:** `frontend/src/lib/useChatStream.ts:379` and `frontend/src/lib/useChatStream.ts:443` (Continue repeats it at `:499`).
   - **Failing sequence:** Let the proxy connection reject before `chatStream` receives an HTTP response or an SSE frame. The normal send simply propagates the rejection from line 379. Regenerate/Continue re-anchor and rethrow at lines 443-449 / 499-503. None invokes `handleStreamError`, which is the only path that clears `isStreaming` and assigns `retryAction: 'regenerate'`. The latch itself releases in `finally`, but its live busy predicate then observes the stranded streaming flag; the user sees no failure bubble and must discover that Stop is the only way to recover. Mid-stream SSE errors after partial text are correctly discriminated; abort and 402/paywall paths correctly remain non-Retry paths.
   - **Suggested fix:** Route non-abort fetch rejections through the same caller-selected error callback as HTTP/SSE errors (and swallow the handled rejection), so cleanup and provenance run once. Keep Abort silent. Add a rejected-fetch test for Send and Regenerate that asserts `isStreaming === false` and exactly one Retry-enabled error bubble.

4. **[NOTE] The document scope token does not order concurrent polls for the same document, so a stale `empty` result can erase the final `ready` result permanently at the poll cap.**
   - **Location:** `frontend/src/lib/useDocumentBrief.ts:42` and `frontend/src/lib/useDocumentBrief.ts:56` (overlapping requests are launched at `:93-95`).
   - **Failing sequence:** On one document, poll 19 remains slow while poll 20 starts. Poll 20 returns `status: 'ready'` and applies it. Poll 19 then returns its older `status: 'empty'`; both requests carry the same scope object, so line 56 accepts the older result and overwrites the ready brief. `pollAttempts` is already 20, therefore the effect at lines 91-92 refuses to schedule another poll and the brief stays hidden until remount/manual refresh. The A -> B -> A object-identity guard works; this is the same-document ordering hole.
   - **Suggested fix:** Serialize refreshes per scope, or add a monotonically increasing request ordinal and apply only the latest request's result. Increment the bounded poll count only for requests actually started. Add a deferred-promise regression in which poll 20 resolves ready before poll 19 resolves empty.

## Checks closed without findings

- F3's latch is released for every return/throw after acquisition because all such paths are inside the outer `try/finally`. It is per mounted `useChatStream`; a document transition clears `sessionId` and unmounts `ChatPanel`, so a dead ref is not shared into the next document. The blocker above is the ungated Send entry point, not a missing `release()`.
- F1's fresh plan reads are scalar statements outside loops; there is no N+1. `normalized_plan(None)` still returns `free`. The later locked plan remains authoritative for the capacity decision and its error detail.
- F2 provenance cannot leak from the four `ChatPanel` operation errors: only the two `handleStreamError` render shapes assign `retryAction`. A restored server assistant message has no UI error provenance, but it still receives the pre-existing last-answer Regenerate action; failures that persisted only the user turn simply have no server-side error message to misclassify. Finding 3 is the live transport exception that never reaches either render shape.
- F5 preserves legacy briefs: `backend/app/api/documents.py:659` returns `status: "ready"` with `coverage.status: "legacy_summary"`, so the `status === 'ready'` render gate accepts them.
- Apart from finding 2's lock-order regression, the slot counts/Rule C behavior, conditional reparse claim, commit-before-dispatch order, terminal error taxonomy, B1 mount boundary, and B4 nudge did not expose another blocker-class sequence.

Review performed from the requested diffs and surrounding source. I did not rerun build, unit, or integration gates.
