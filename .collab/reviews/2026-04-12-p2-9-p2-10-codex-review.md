# P2-9/P2-10 审阅

A. 未发现 `-O`/`PYTHONOPTIMIZE`（查 Dockerfile/entrypoint/全仓库），`assert` 生效。  
B. 建议：`if last_exc is not None: raise last_exc`，否则 `raise RuntimeError`，避免 `-O` 或 `_max_retries=0`。  
C. `type: ignore[attr-defined]`（Qdrant 类型差异）可保留；`noqa: I001/E731` 去掉会触发 Ruff，可保留。  
D. 两端点 `Request` 注入正确，路由加载正常。  
E. 未见 bypass 漏洞：仅有效 JWT 才有 `user`；无/坏 token 都走匿名限流。  
F. `120/min/IP` 对 demo 搜索/chunk 详情偏宽松但可接受。  
G. 建议后续补 `user_id` 维度限流，防注册账号滥用（后续 PR）。  
H. 结论：P2-9 建议按 B 小改后合并；P2-10 可合并。
