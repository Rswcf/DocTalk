APPROVE. The one-line change (`setHtml(null)` at the start of the `CodeBlock` `useEffect`) correctly clears previously rendered highlighted HTML whenever `code` or `language` changes, so the component shows the plain `<pre>` fallback until new highlighting resolves, and with the existing `cancelled` guard it also avoids stale async updates from prior runs overwriting current state.